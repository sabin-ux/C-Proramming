#include <stdio.h>
#include <math.h>
int main(){

 int sec, min, hour, sec1, min1, hour1, sec2, min2, hour2;
 printf("Enter the time in seconds:\n");
 scanf("%d", &sec);
    hour= sec/3600;
    hour1=sec%3600;
    min=hour1/60;
    min1=hour1%60;
    sec = min/60;
 printf("The time in hours %d, minutes %d and seconds is %d:\n",hour,min,sec);
    return 0;
}